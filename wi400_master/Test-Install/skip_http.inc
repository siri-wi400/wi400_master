<?php
require_once "function_test.php";
if (!isset($settings['http_port']) || $settings['http_port']=="") die("skip HTTP configurare 'http_port'");
if (!isset($settings['http_server']) || $settings['http_server']=="") die("skip HTTP configurare 'http_server'");
?>

